// Event listeners voor extra functionaliteit
document.addEventListener('DOMContentLoaded', function() {
    // Voorbeeld: Activeer eerste accordion item
    const firstAccordion = new bootstrap.Collapse(document.getElementById('collapseStarters'), {
        toggle: true
    });

    // Andere custom JavaScript kan hier komen
    // Bijvoorbeeld: Filterfunctionaliteit, speciale animaties, etc.

    console.log('Menukaart geladen - Pizzeria Avenida');
});